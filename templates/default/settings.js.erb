/*
	settings.js
	You should set these before use
	Username
		Username of airfield
	Password
		Password of airfield
	Port
		Port of airfield
	cookieSecret
		secret which is used to encrypt cookies
	sessionSecret
		^ same than earlier but for sessions
	enable_openstack
		If you want to enable openstack support then change this to true
	os_api_username
		Openstack username (only required if openstack is enabled)
	os_api_password
		Openstack password (only required if openstack is enabled)
	os_api_tenant
		Airfield is fetching info about every instance but this is needed for authentication
	os_keystone_url
		Keystone api url
	os_nova_url
		Nova api url
*/

exports.settings = {
    username: "<%= @admin_user %>",
    password: "<%= @admin_pass %>",
    port: <%= @listen_port %>,
    cookieSecret: "<%= @cookie_secret %>",
    sessionSecret: "<%= @session_secret %>",
    enable_openstack: <%= @openstack %>,
    os_api_username: "<%= @os_user %>",
    os_api_password: "<%= @os_password %>",
    os_api_tenant: "<%= @os_tenant %>",
    os_keystone_url: "<%= @os_keystone_url %>",
    os_nova_url: "<%= @os_nova_url %>",
    REDIS_PORT: <%= @redis_port %>,
    REDIS_HOST: '<%= @redis_host %>'
};